# 可选：托管 / 公共体验模式（默认不需要，留空即为开源「自带 Key」模式）。
#
# 设置 VITE_DEFAULT_API_KEY 后，站点进入「托管模式」：
#   - 设置弹窗隐藏 API Key / Base URL 输入，改用下面注入的值
#   - 用户只能从 VITE_DEFAULT_MODELS 白名单里「选模型」
#   - 请求走你注入的 Key（即你替所有访客付费）
#
# ⚠️ 纯前端无后端：构建时 Key 会被内联进 JS，部署后可被访客从浏览器提取。
#    请务必使用「限额 / 预充值」的专用 Key（如 OpenRouter 限额 Key），把最坏损失封顶。
#
# 用法：复制本文件为 .env.local（本地开发，已被 .gitignore），
#      或把这些变量填到托管平台（Vercel / Netlify / Cloudflare）的环境变量里。
#      切勿把含真实 Key 的 .env / .env.local 提交到仓库！

VITE_DEFAULT_API_KEY=
VITE_DEFAULT_BASE_URL=https://openrouter.ai/api/v1
VITE_DEFAULT_MODELS=openai/gpt-4o-mini,deepseek/deepseek-chat
